/// Product branding — the single place the user-facing name lives.
///
/// The repo/package stays `fitai`; only display strings read from here, so a
/// rename (e.g. to "Goose Trainer") is a one-line change.
abstract final class Brand {
  static const appName = 'Sargent AI';
  static const tagline = 'Your AI training sergeant — on your six, every set.';
}
